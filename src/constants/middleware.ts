import { Routes } from "@/types/middleware";

export const authRoutes: Routes = [
  "/login",
  "/register",
  "/forget-password",
  "/update-password",
  "/test",
];

export const publicRoutes: Routes = [];
